# shmily-drag-image

图片拖拽排序组件，支持字符串或对象数组、自定义添加动作和删除确认。

```vue
<shmily-drag-image v-model="images" keyName="src" />
```

上游：<https://github.com/shmilyany/shmily-drag-image>，vendored 版本 `2.0.2`。随附文件未声明许可证，`THIRD_PARTY_NOTICES` 和 `SBOM.spdx.json` 因此标记为 `NOASSERTION`。
